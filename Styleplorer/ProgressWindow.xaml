<Window x:Class="Styleplorer.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Копирование" Height="150" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="StatusText" Text="Копирование..." Margin="0,0,0,5"/>
        <ProgressBar x:Name="ProgressBar" Height="20" Grid.Row="1" Margin="0,0,0,5"/>
        <TextBlock x:Name="ProgressText" Text="0%" Grid.Row="2" HorizontalAlignment="Center"/>
    </Grid>
</Window>
